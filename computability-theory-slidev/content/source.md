## 一、计算性与复杂性

**Decidable / Recognizable**：问的是“到底能不能用算法解决”，属于**可计算性理论**。

### 1. Recognizable：能认出“是”，但未必认得出“不是”

一个问题是 **Recognizable（可识别的 / Turing-recognizable）**，意思是存在一个程序 $M$：

$$
x\in L\Rightarrow M(x)\text{ 最终接受}
$$

但是如果

$$
x\notin L
$$

程序可能：

- 返回“不接受”；
- **也可能永远运行下去。**

所以它只能保证：

> **如果答案是 YES，我最终一定能告诉你；如果答案是 NO，我可能永远算不完。**

Cornell 对 recognizability 的定义正是如此。

例如经典的图灵机接受问题

$$
A_{TM}=
\{\langle M,w\rangle:M\text{ 接受 }w\}
$$

就是可识别的：直接模拟 $M(w)$。

如果 $M$ 接受 $w$，总有一天能看到它接受；但如果它不接受，$M$ 可能一直运行，因此我们可能永远不知道答案。

### 2. Decidable：无论 YES 还是 NO，都一定停下来

**Decidable（可判定）**的要求更强。

存在算法 $M$，对于**所有输入**都保证有限时间停机：

$$
\begin{cases}
x\in L &\Rightarrow M(x)=YES\\
x\notin L &\Rightarrow M(x)=NO
\end{cases}
$$

因此：

> **可判定 = 一定能算出 YES/NO，而且一定会结束。**

这直接意味着

$$
\boxed{Decidable\subseteq Recognizable}
$$

而且这个包含是严格的：

$$
\boxed{Decidable\subsetneq Recognizable}
$$

因为存在“可识别但不可判定”的问题，例如图灵机接受问题。Cornell 的讲义也明确指出 decidability 是比 recognizability 更强的要求，并给出了这种严格区别。

你可以把两者区别记成：

| 输入情况 | Recognizable | Decidable |
|---|---|---|
| YES | 一定停机并回答 YES | 一定停机并回答 YES |
| NO | **可能死循环** | 一定停机并回答 NO |

### 3. 图灵停机问题

图灵机指的是描述某一特定算法的机器

语言 $L$ 是图灵可识别的，若存在图灵机 $M$，使得对于任意字符串 $w$：

- 若 $w \in L$，$M(w)$ 在有限步内停机并**接受**。
- 若 $w \notin L$，$M(w)$ **拒绝停机** 或 **无限循环**。

此时 $M$ 称为 $L$ 的识别器（Recognizer）。

图灵机接受问题（Acceptance Problem）定义为语言：

$$A_{TM} = \{ \langle M, w \rangle \mid M \text{ 是一台图灵机，且 } M \text{ 接受输入 } w \}$$

#### 3.1 为什么 $A_{TM}$ 是可识别的？

我们可以设计一个非常直接的程序：

```
输入 <M,w>

运行/模拟 M(w)

如果 M(w) 停机：
    接受
```

这里甚至不管 M 最后是“接受”还是“拒绝”，只要它**停下来了**，我们就知道答案是：
$$
⟨M,w⟩∈HALT
$$
因此 HALT 是 **recognizable（可识别的）**。

#### 3.2 为什么 $A_{TM}$ 是不可判定的？

证明核心在于：**无法构造一个算法，在有限时间内区分“正在进行漫长但终将接受的计算”与“陷入了无限循环”。**

通过康托尔对角线法（反证法）证明：

1. **反设**：假设 $A_{TM}$ 是可判定的，则存在判定器 $H$：

   $$H(\langle M, w \rangle) = \begin{cases} \text{接受}, & \text{若 } M \text{ 接受 } w \\ \text{拒绝}, & \text{若 } M \text{ 不接受 } w \end{cases}$$

   由于 $H$ 是判定器，$H$ 面对任意输入都保证在有限步内停机。

2. **构造对抗图灵机 $D$**：以任意图灵机自身编码 $\langle M \rangle$ 作为输入：

   - $D$ 调用子程序 $H$ 运行在 $\langle M, \langle M \rangle \rangle$ 上。
   - 若 $H$ 输出“接受”，$D$ 强制进入自身拒绝状态或死循环；
   - 若 $H$ 输出“拒绝”，$D$ 停机并接受。

3. **引入自指冲突**：将 $D$ 自身的编码 $\langle D \rangle$ 作为输入传入 $D$：

   $$D(\langle D \rangle) = \begin{cases} \text{接受}, & \text{当且仅当 } H(\langle D, \langle D \rangle \rangle) \text{ 拒绝（即 } D \text{ 不接受 } \langle D \rangle \text{）} \\ \text{拒绝}, & \text{当且仅当 } H(\langle D, \langle D \rangle \rangle) \text{ 接受（即 } D \text{ 接受 } \langle D \rangle \text{）} \end{cases}$$

4. **导出矛盾**：

   $$D \text{ 接受 } \langle D \rangle \iff D \text{ 不接受 } \langle D \rangle$$

   该矛盾推翻了假设，因此判定器 $H$ 不存在，$A_{TM} \notin R$。

## 二、计算复杂度

**计算复杂度（computational complexity）**研究的是：

> 当输入规模 $n$ 变大时，一个算法需要的计算资源（主要是时间、空间）如何增长。

因此它并不太关心“这台电脑到底跑 0.1 秒还是 1 秒”，而更关心增长规律。例如 MIT 将 $P$ 定义为能在 $n^c$ 形式的多项式时间内解决的问题，而 Stanford 的复杂度课程也强调：多项式增长相对温和，指数增长会随着输入规模增加而迅速失控。

### 1. 什么叫多项式复杂度？

如果运行时间大致满足

$$
T(n)=O(n^k)
$$

其中 $k$ 是某个**固定常数**，就叫多项式时间。

比如：

$$
O(n),\quad O(n^2),\quad O(n^3),\quad O(n^{100})
$$

理论上都属于多项式时间。

一个最简单的例子：**在 $n$ 个数中找最大值**。

假设：

```text
3  8  2  10  5
```

从左到右扫一遍即可：

```text
当前最大 = 3
看到 8  → 更新为 8
看到 2  → 不变
看到 10 → 更新为 10
看到 5  → 不变
```

大概需要检查 $n$ 次，所以

$$
T(n)\sim n
$$

即

$$
\boxed{O(n)}
$$

这是多项式时间。

即使写成两个循环：

```python
for i in range(n):
    for j in range(n):
        ...
```

执行次数大约为

$$
n\times n=n^2
$$

所以

$$
\boxed{O(n^2)}
$$

仍然是多项式时间。

因此一个非常实用的判断方式是：

> **固定层数的 $n$ 次循环，通常产生 $n^k$，属于多项式。**

### 2. 什么叫指数复杂度？

指数复杂度典型形式是：

$$
T(n)=O(2^n)
$$

或者

$$
O(3^n),O(1.5^n)
$$

典型例子是 SAT：

$$
(x_1\lor x_2)\land(\neg x_1\lor x_3)\land\cdots
$$

问题：

> 是否存在一组 TRUE/FALSE 赋值，使整个公式为 TRUE？

可以把

$$
2^n
$$

种赋值全部试一遍，则是：

$$
O(2^n)
$$

### 3. P：不仅能解决，而且能比较快地解决

现在进入复杂度理论。

**P（Polynomial Time）**表示存在一个**确定性算法**，能够在多项式时间内解决问题：

$$
T(n)=O(n^k)
$$

其中 $k$ 是某个常数。

比如

$$
O(n),\quad O(n^2),\quad O(n^5)
$$

都属于多项式时间。

MIT 将 P 描述为所有“polynomial-time decidable languages”，Cornell 也给出了同样的定义。

例如决策版本：

> 给定图 $G$、两个点 $s,t$ 和 $k$，是否存在长度不超过 $k$ 的路径？

可以用多项式时间算法解决，因此属于 P。

所以显然

$$
\boxed{P\subseteq Decidable}
$$

因为如果一个算法连多项式时间都保证结束，它当然是“保证有限时间结束”的。

### 4. NP：给我一个答案，我能很快检查

NP 是 **Nondeterministic Polynomial Time**，不是“Non-Polynomial”。

形式定义可以说：

> 能被非确定性图灵机在多项式时间内解决的问题。

更直观而常用的理解是：

> 对于 YES 实例，如果别人给你一个合适的“证据 certificate”，你能够在多项式时间内验证它。

> 

我们目前不知道是否存在通用的确定性多项式时间算法来找到答案。

但是如果别人告诉你：

$$
x_1=T,\qquad x_2=F,\qquad x_3=T,\dots
$$

你只需要把这些值代进去检查即可，很快就能判断这个答案对不对。

因此 SAT 属于 NP。实际上 SAT 是 NP-complete。

### 5. 为什么 $P\subseteq NP$？

因为：

> 如果我本来就能快速地**算出答案**，那我当然也能快速地**验证答案**。

因此：

$$
\boxed{P\subseteq NP}
$$

但现在最大的问题是：

$$
\boxed{P=NP\ ?}
$$

我们不知道。

也就是说我们不知道：

> “答案可以快速验证”

是否必然意味着

> “答案可以快速找到”。

### 6. 为什么 $NP\subseteq Decidable$？

这一点特别重要。

即使一个 NP 问题不知道怎么**快速**求解，也仍然可以暴力搜索。

例如 SAT 有 $n$ 个布尔变量，可以把

$$
2^n
$$

种赋值全部试一遍。

可能非常慢：

$$
O(2^n)
$$

但它终究会结束。

所以 SAT 虽然可能不在 P，却一定是 **Decidable**。Cornell 的可计算性讲义就特别用 SAT 举这个例子：即使暴力枚举需要指数时间，它仍然是有限时间，因此 SAT 是 decidable。

因此要特别区分：

$$
\boxed{\text{很慢}\neq\text{不可计算}}
$$

## 三、图灵完备

如果一种机器强到足以**模拟任意图灵机**，那么我们就称它：

$$
\boxed{\text{Turing Complete}}
$$

直观上就是：

> **只要某件事情存在算法可以完成，这个系统原则上就能写出程序完成它。**

这里必须加两个条件：

$$
\boxed{\text{足够的时间 + 理论上无限的存储空间}}
$$

现实电脑的内存当然有限，所以严格来说现实机器不是数学意义上真正拥有无限纸带的图灵机；我们通常把它们理想化为可以不断扩展存储空间的机器。

例如常见的通用编程语言都可以看作具有这种计算能力。甚至一些非常奇怪的系统，例如 Conway 的 Game of Life，也能模拟通用图灵机，因此具有通用计算能力。

但“图灵完备”绝对不意味着“什么问题都能解决”

准确说法是：

$$
\boxed{
\text{图灵完备}
=
\text{能完成任何“可计算”的计算}
}
$$

程序语言是对底层的抽象，程序语言决定了底层的行为，因此，对于程序语言，我们也可以讨论图灵完备。对于普通命令式语言来说，如果它能够做到下面三件事情，通常就已经足以构造图灵完备系统：

$$
\boxed{
\text{可扩展存储}
+
\text{条件分支}
+
\text{不受固定上限限制的循环/递归}
}
$$

可以从计算过程本身理解。

假设我要计算：

$$
100! =
100\times99\times\cdots\times1.
$$

需要保存一些中间结果。

输入进一步变成：

$$
n!
$$

那么随着 $n$ 不断增大：

- 中间数据可能越来越大；
- 计算步骤越来越多。

一个通用计算模型不能规定：

$$
n\le10000
$$

或者：

$$
\text{最多运行 }10^9\text{ 步}.
$$

否则总能构造一个问题超过这个限制。

因此通用计算理论需要把：

$$
\boxed{\text{存储空间}}
$$

和

$$
\boxed{\text{运行时间}}
$$

都视为原则上没有固定上限。


