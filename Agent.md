## 内容编辑的结构检查

### 标题层级必须完整

- 不要在一个父标题下只创建单独的 `3.1` 子标题；这会造成不完整、突兀的层级结构。
- 如果一个内容需要编号为 `3.1`，应确认存在至少一个并列子点（例如 `3.2`），或者改用不编号的小标题／普通段落。
- 重构文章后，检查章节标题的编号是否连续，并检查每一层标题的层级是否自然、对称。

### 本次应用

“可微”已组织为：

1. `3.1 多元标量函数的可微`
2. `3.2 向量值函数的可微与 Jacobian 矩阵`
